const initState = {
    users: [
      {
        name: "firas",
        lastname: "bchir",
      },
    ],
    username: "firas-bc",
  };
  const rootReducers = (state = initState, action) => {
    return state;
  };
  export default rootReducers;